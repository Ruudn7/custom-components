import { WeekByDatePipe } from './week-by-date.pipe';

describe('WeekByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new WeekByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
